export default function AdminLoginPage() {
  return (
    <main className="page-shell">
      <section className="panel">
        <p className="meta">Admin Login</p>
        <h1>Administration is pre-seeded through environment configuration.</h1>
        <p>Admin accounts use the same auth flow as normal users, with a different role.</p>
      </section>
    </main>
  );
}
